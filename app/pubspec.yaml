name: stock_screener
description: "주식 스크리너 결과 뷰어 (미국/국내, 단기/중기/장기)"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.13.3

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.3.0
  webview_flutter: ^4.10.0
  url_launcher: ^6.3.0
  flutter_local_notifications: ^22.3.0
  android_alarm_manager_plus: ^5.1.1
  permission_handler: ^12.0.0
  android_intent_plus: ^6.1.0
  intl: ^0.20.0
  timezone: ^0.11.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
